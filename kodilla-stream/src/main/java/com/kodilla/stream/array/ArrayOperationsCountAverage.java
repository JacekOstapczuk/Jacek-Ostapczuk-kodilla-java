/*

package com.kodilla.stream.array;

import java.util.stream.IntStream;


public class ArrayOperationsCountAverage implements  ArrayOperations  {



    public  double getAverage(int[] numbers){

        double numbersToCalculate = IntStream.range(0,numbers.length)
                .map (n->(double)n)
               .average();


        return (double)numbersToCalculate;
    }
}


 */